# Basic Auth credentials for httpath
# Copy this file to .env and fill in your values.
# Both variables must be set to enable auth; setting only one disables it with a warning.

HTTPATH_USER=admin
HTTPATH_PASS=your-password-here
